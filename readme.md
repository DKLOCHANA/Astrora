Astrune — Legal & Support site.

Structure:
- `/` — home (legal & support hub)
- `/privacy-policy/` — Privacy Policy
- `/terms-of-service/` — Terms of Service
- `/contact/` — Contact form

Before going live:
- Replace `YOUR_FORM_ID` in `contact/index.html` with a real Formspree form ID (or swap in your own form backend).
- Update `support@astruneapp.com` throughout if you use a different support address.
